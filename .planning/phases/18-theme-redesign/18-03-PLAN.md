---
phase: 18-theme-redesign
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/components/templates/TemplateCard.tsx
  - src/components/templates/TemplateBuilder.tsx
  - src/components/templates/TemplateList.tsx
  - src/components/templates/ExerciseRow.tsx
  - src/components/templates/ExerciseList.tsx
  - src/components/history/ExerciseHistory.tsx
  - src/components/history/PRIndicator.tsx
  - src/components/history/PRList.tsx
  - src/components/history/EstimatedMaxDisplay.tsx
  - src/components/rotation/QuickStartCard.tsx
  - src/components/rotation/RotationEditor.tsx
autonomous: true

must_haves:
  truths:
    - "Template cards have rounded corners and match warm theme"
    - "Exercise history views use updated radius tokens"
    - "Quick Start card and rotation editor match the soft aesthetic"
  artifacts:
    - path: "src/components/templates/TemplateCard.tsx"
      provides: "Template card with rounded-2xl"
      contains: "rounded-2xl"
    - path: "src/components/rotation/QuickStartCard.tsx"
      provides: "Quick start with rounded-2xl"
      contains: "rounded-2xl"
  key_links:
    - from: "src/index.css"
      to: "src/components/templates/"
      via: "Token consumption"
      pattern: "rounded-2xl|rounded-xl"
---

<objective>
Apply the warm theme to template components, exercise history components, and rotation components.

Purpose: Templates, history, and rotation are secondary flows that users interact with regularly. Consistent theming across these surfaces ensures the redesign feels complete, not partial.
Output: 11 files updated with warm rounded corners and softened borders.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-theme-redesign/18-CONTEXT.md
@.planning/phases/18-theme-redesign/18-01-SUMMARY.md
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep template and history components</name>
  <files>
    src/components/templates/TemplateCard.tsx
    src/components/templates/TemplateBuilder.tsx
    src/components/templates/TemplateList.tsx
    src/components/templates/ExerciseRow.tsx
    src/components/templates/ExerciseList.tsx
    src/components/history/ExerciseHistory.tsx
    src/components/history/PRIndicator.tsx
    src/components/history/PRList.tsx
    src/components/history/EstimatedMaxDisplay.tsx
  </files>
  <action>
Read ALL files first, then apply:

**For ALL files:**
- Replace `rounded-lg` with `rounded-2xl` for card-like containers
- Replace `rounded-lg` with `rounded-xl` for buttons, inputs, badges, and smaller elements
- Soften borders where cards use them for depth (change to border-border-primary or remove if parent Card component provides shadow)
- Keep borders that serve as list item dividers (border-b between items in a list)

**Specific attention:**
- **TemplateBuilder.tsx:** Has 4 rounded-lg occurrences. The builder is a form with exercise rows -- use rounded-2xl for the outer container and rounded-xl for inner elements.
- **TemplateCard.tsx:** Likely uses Card component. Update any local overrides.
- **ExerciseRow.tsx (templates):** Has drag-drop -- do NOT change dnd-kit-related classes or data attributes.
- **ExerciseHistory.tsx:** Has 2 rounded-lg. Update both.
- **PRIndicator.tsx, PRList.tsx, EstimatedMaxDisplay.tsx:** Read first -- may have badges/tags that should use rounded-xl or keep rounded-full for pill badges.

**Do NOT change:**
- Functional behavior, handlers, state
- rounded-full (intentional pill shapes for badges)
- Data-testid attributes
- dnd-kit drag/drop attributes and handlers
  </action>
  <verify>
`npm run build` succeeds. `grep -r "rounded-lg" src/components/templates/ src/components/history/` returns 0 matches.
  </verify>
  <done>All template and history files use rounded-2xl/rounded-xl. No rounded-lg remains. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep rotation components</name>
  <files>
    src/components/rotation/QuickStartCard.tsx
    src/components/rotation/RotationEditor.tsx
  </files>
  <action>
Read both files first, then apply:

**QuickStartCard.tsx:** Has 4 rounded-lg occurrences. This is the prominent "start workout" card on the Workouts tab.
- Replace `rounded-lg` with `rounded-2xl` for the main card container
- Replace `rounded-lg` with `rounded-xl` for buttons and inner elements
- If it doesn't use the Card component, consider adding `shadow-[var(--shadow-card)]` or inline style for shadow

**RotationEditor.tsx:** Has 1 rounded-lg.
- Replace `rounded-lg` with `rounded-2xl`
- Soften borders if present

**Do NOT change:**
- Functional behavior
- Layout structure
  </action>
  <verify>
`npm run build` succeeds. `grep -r "rounded-lg" src/components/rotation/` returns 0 matches.
  </verify>
  <done>Both rotation files use rounded-2xl/rounded-xl. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `grep -r "rounded-lg" src/components/templates/ src/components/history/ src/components/rotation/` returns 0 matches
3. No functional changes -- all event handlers and state management untouched
</verification>

<success_criteria>
- All 9 template/history files updated with rounded-2xl/rounded-xl
- Both rotation files updated
- No rounded-lg remains in templates/, history/, or rotation/ directories
- Build compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/18-theme-redesign/18-03-SUMMARY.md`
</output>
