---
phase: 18-theme-redesign
plan: 05
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/components/settings/DemoDataSection.tsx
  - src/components/settings/ObservabilitySection.tsx
  - src/components/settings/DataQualitySection.tsx
  - src/components/settings/RotationSection.tsx
  - src/components/settings/ToonExportSection.tsx
  - src/components/backup/BackupSettings.tsx
  - src/components/backup/BackupReminder.tsx
autonomous: true

must_haves:
  truths:
    - "All settings sections have rounded corners and warm styling"
    - "DemoDataSection hardcoded gradient colors updated to warm palette"
    - "Backup components match new theme"
  artifacts:
    - path: "src/components/settings/DemoDataSection.tsx"
      provides: "Demo data section with warm gradient colors"
      contains: "rounded-2xl"
    - path: "src/components/backup/BackupSettings.tsx"
      provides: "Backup settings with warm theme"
      contains: "rounded-2xl"
  key_links:
    - from: "src/index.css"
      to: "src/components/settings/DemoDataSection.tsx"
      via: "Hardcoded oklch gradient colors that must match new palette"
      pattern: "oklch\\(0\\.65|oklch\\(0\\.60"
---

<objective>
Apply the warm theme to all settings sections and backup components, including fixing hardcoded gradient colors in DemoDataSection.

Purpose: Settings and backup screens need consistent theming. DemoDataSection contains hardcoded OKLCH gradient colors that must be aligned with the new warm palette to avoid jarring visual inconsistency.
Output: 7 files updated with warm theme, hardcoded gradient colors fixed.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-theme-redesign/18-CONTEXT.md
@.planning/phases/18-theme-redesign/18-01-SUMMARY.md
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep settings components and fix hardcoded colors</name>
  <files>
    src/components/settings/DemoDataSection.tsx
    src/components/settings/ObservabilitySection.tsx
    src/components/settings/DataQualitySection.tsx
    src/components/settings/RotationSection.tsx
    src/components/settings/ToonExportSection.tsx
  </files>
  <action>
Read ALL files first, then apply:

**For ALL files:**
- Replace `rounded-lg` with `rounded-2xl` for card-like containers
- Replace `rounded-lg` with `rounded-xl` for buttons, inputs, smaller elements
- Soften borders where appropriate

**DemoDataSection.tsx -- CRITICAL hardcoded colors:**
- Has 8 rounded-lg occurrences -- update all
- Find `oklch(0.65_0.18_60)` and `oklch(0.60_0.15_35)` (hardcoded gradient colors)
- Replace with warm-palette-aligned values. Since the hue ~60 happens to already be warm-ish, verify they look harmonious with the new bg tokens. If the chroma is too high for the new muted aesthetic, reduce it (e.g., oklch(0.65 0.12 60), oklch(0.60 0.10 45)).
- Use your discretion on exact values -- goal is harmony with the new warm background

**ObservabilitySection.tsx:** Has 4 rounded-lg -- update all
**ToonExportSection.tsx:** Has 6 rounded-lg -- update all
**RotationSection.tsx:** Has 5 rounded-lg -- update all
**DataQualitySection.tsx:** Has 2 rounded-lg -- update both
  </action>
  <verify>
`npm run build` succeeds. `grep -r "rounded-lg" src/components/settings/` returns 0 matches. `grep "oklch(0.65_0.18_60)\|oklch(0.60_0.15_35)" src/components/settings/DemoDataSection.tsx` returns 0 matches (original hardcoded values replaced).
  </verify>
  <done>All 5 settings files use rounded-2xl/rounded-xl. DemoDataSection gradient colors updated to match warm palette. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep backup components</name>
  <files>
    src/components/backup/BackupSettings.tsx
    src/components/backup/BackupReminder.tsx
  </files>
  <action>
Read both files first, then apply:

**BackupSettings.tsx:** Has 5 rounded-lg occurrences.
- Replace `rounded-lg` with `rounded-2xl` for card containers
- Replace `rounded-lg` with `rounded-xl` for buttons, inputs
- Soften borders

**BackupReminder.tsx:**
- Replace any `rounded-lg` with `rounded-2xl`
- This is likely a banner/alert -- keep warm background, update radius

**Do NOT change:**
- Functional behavior
- Alert/warning colors (semantic colors stay saturated)
  </action>
  <verify>
`npm run build` succeeds. `grep -r "rounded-lg" src/components/backup/` returns 0 matches.
  </verify>
  <done>Both backup files use rounded-2xl/rounded-xl. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `grep -r "rounded-lg" src/components/settings/ src/components/backup/` returns 0 matches
3. DemoDataSection no longer has hardcoded oklch gradient colors that clash with warm palette
</verification>

<success_criteria>
- All 5 settings files updated with rounded-2xl/rounded-xl
- Both backup files updated
- DemoDataSection hardcoded gradient colors aligned with warm palette
- No rounded-lg remains in settings/ or backup/ directories
- Build compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/18-theme-redesign/18-05-SUMMARY.md`
</output>
