---
phase: 18-theme-redesign
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/components/Navigation.tsx
  - src/App.tsx
  - src/components/DeleteConfirmation.tsx
  - src/components/ExerciseForm.tsx
  - src/components/ExerciseList.tsx
  - src/components/GymForm.tsx
  - src/components/GymList.tsx
  - src/components/workout/ActiveWorkout.tsx
  - src/components/workout/ExerciseView.tsx
  - src/components/workout/SetRow.tsx
  - src/components/workout/SetGrid.tsx
  - src/components/workout/SetLogger.tsx
  - src/components/workout/StartWorkout.tsx
  - src/components/workout/RecentWorkoutCard.tsx
  - src/components/workout/WorkoutComplete.tsx
  - src/components/workout/RestTimer.tsx
  - src/components/workout/ProgressionAlert.tsx
  - src/components/workout/ExerciseSubstitution.tsx
autonomous: true

must_haves:
  truths:
    - "Navigation bar has warm background, rounded active indicator, and soft shadow"
    - "All workout screens have rounded corners and match the new warm aesthetic"
    - "Form components (exercise, gym) use updated radius and warm styling"
  artifacts:
    - path: "src/components/Navigation.tsx"
      provides: "Redesigned tab bar with warm treatment"
      contains: "shadow"
    - path: "src/components/workout/WorkoutComplete.tsx"
      provides: "Post-workout summary with rounded cards"
      contains: "rounded-2xl"
  key_links:
    - from: "src/index.css"
      to: "src/components/Navigation.tsx"
      via: "Token consumption for warm backgrounds"
      pattern: "bg-bg-primary|shadow"
---

<objective>
Apply the warm theme to the Navigation bar, top-level form components, and all workout components.

Purpose: Navigation is the most visible element (always on screen). Workout components are the most frequently used feature. Together they define the user's moment-to-moment experience of the new theme.
Output: 18 files updated with warm rounded corners, shadows (where appropriate), and border cleanup.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-theme-redesign/18-CONTEXT.md
@.planning/phases/18-theme-redesign/18-01-SUMMARY.md
@src/index.css
@src/components/Navigation.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Navigation bar and top-level components</name>
  <files>
    src/components/Navigation.tsx
    src/App.tsx
    src/components/DeleteConfirmation.tsx
    src/components/ExerciseForm.tsx
    src/components/ExerciseList.tsx
    src/components/GymForm.tsx
    src/components/GymList.tsx
  </files>
  <action>
**Navigation.tsx -- Full restyle (not just token swap):**
- Change background from `bg-bg-primary` to `bg-bg-secondary` (slightly elevated from page)
- Remove `border-t border-border-primary` (replace with shadow)
- Add upward shadow: use `shadow-[var(--shadow-nav)]` or inline style with var(--shadow-nav)
- Active tab indicator: Replace `border-t-2 border-accent -mt-px` with a rounded pill background behind the active text. Use something like: wrap each button text in a span, and for active state apply `bg-accent/15 rounded-xl px-4 py-1 text-accent` instead of border-top. Inactive state: `text-text-muted hover:text-text-secondary`.
- Increase button padding slightly for touch targets
- Keep all data-testid attributes unchanged

**App.tsx:**
- Read file first. Apply `rounded-2xl` to any container divs that use `rounded-lg`.
- Keep layout structure intact.

**DeleteConfirmation.tsx:**
- Read file first. It likely uses Dialog or similar pattern.
- Update `rounded-lg` to `rounded-2xl`, add shadow if it's a standalone modal.

**ExerciseForm.tsx, GymForm.tsx:**
- Read files first. Replace `rounded-lg` with `rounded-2xl`.
- Soften borders (change border-border-secondary to border-border-primary where used).

**ExerciseList.tsx, GymList.tsx:**
- Read files first. Replace `rounded-lg` with `rounded-2xl`.
- If items have border dividers, keep them (list items use borders for separation, not elevation).
  </action>
  <verify>
`npm run build` succeeds. `grep "border-t border-border-primary" src/components/Navigation.tsx` returns 0 matches. Navigation uses shadow instead of top-border.
  </verify>
  <done>Navigation has warm background, rounded pill active indicator, and shadow. Top-level form/list components use rounded-2xl.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep workout components with warm theme</name>
  <files>
    src/components/workout/ActiveWorkout.tsx
    src/components/workout/ExerciseView.tsx
    src/components/workout/SetRow.tsx
    src/components/workout/SetGrid.tsx
    src/components/workout/SetLogger.tsx
    src/components/workout/StartWorkout.tsx
    src/components/workout/RecentWorkoutCard.tsx
    src/components/workout/WorkoutComplete.tsx
    src/components/workout/RestTimer.tsx
    src/components/workout/ProgressionAlert.tsx
    src/components/workout/ExerciseSubstitution.tsx
  </files>
  <action>
Read ALL workout component files first, then apply these mechanical changes:

**For ALL files:**
- Replace `rounded-lg` with `rounded-2xl` for card-like containers
- Replace `rounded-lg` with `rounded-xl` for buttons and inputs within these components
- Remove unnecessary border classes on card-like elements (they now have shadows from Card.tsx)
- Keep border classes for list item separators (border-b, border-t for dividers between items)

**Specific attention:**
- **SetGrid.tsx / SetRow.tsx:** These are the batch logging grid. Soften cell borders. Use `rounded-xl` for cells, `border-border-primary` (softer) instead of `border-border-secondary`. Keep the grid structure intact.
- **WorkoutComplete.tsx:** This has 10 occurrences of rounded-lg. Update all to rounded-2xl for cards, rounded-xl for smaller elements like badges/tags.
- **ExerciseSubstitution.tsx:** Has 6 occurrences of rounded-lg. Update based on element type (cards vs inputs).
- **SetLogger.tsx:** Has 3 occurrences. Update similarly.
- **RecentWorkoutCard.tsx:** Uses Card component already (inherits new styles). Only update any local rounded-lg if present.

**Do NOT change:**
- Functional behavior (onClick handlers, state, etc.)
- Tailwind layout classes (flex, grid, gap, etc.)
- Color classes for semantic states (text-success, text-error, bg-accent)
- Data-testid attributes
  </action>
  <verify>
`npm run build` succeeds. `grep -r "rounded-lg" src/components/workout/` returns 0 matches.
  </verify>
  <done>All 11 workout component files use rounded-2xl (cards) or rounded-xl (inputs/buttons). No rounded-lg remains in workout directory. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `grep -r "rounded-lg" src/components/Navigation.tsx src/components/workout/` returns 0 matches
3. Navigation no longer has `border-t` -- uses shadow instead
4. Navigation active state uses rounded pill background, not border-top
</verification>

<success_criteria>
- Navigation bar fully restyled with warm background, shadow, and rounded pill active indicator
- All 11 workout component files updated with rounded-2xl/rounded-xl
- Top-level form/list components (Exercise, Gym, Delete) updated
- No rounded-lg remains in any modified file
- Build compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/18-theme-redesign/18-02-SUMMARY.md`
</output>
