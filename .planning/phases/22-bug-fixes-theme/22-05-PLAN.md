---
phase: 22-bug-fixes-theme
plan: 05
type: execute
wave: 3
depends_on: ["22-02", "22-03", "22-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "App visually presents a clean blue/teal aesthetic with no orange remnants"
    - "All text is readable against all backgrounds"
    - "Charts display correctly with teal palette"
    - "Quick Start works when rotation and gym are configured"
    - "TypeScript build completes with zero errors"
  artifacts: []
  key_links: []
---

<objective>
Final verification sweep: build check, color remnant scan, and visual inspection checkpoint.

Purpose: Ensures no orange remnants slipped through, build is clean, and the visual result matches the Apple dark mode teal aesthetic described in CONTEXT.md.
Output: Verified clean build with no color remnants, visual approval from user.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bug-fixes-theme/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification sweep</name>
  <files></files>
  <action>
Run a comprehensive set of automated checks:

1. **Build check:** `npx tsc --noEmit` must pass with zero errors
2. **Vite build:** `npx vite build` must succeed
3. **Orange remnant scan:** `grep -rn "orange\|#[fF][fF][89aA]\|oklch.*0\.1[5-9].*45\|hsl.*16.*100" src/ --include="*.tsx" --include="*.ts" --include="*.css"` — document any remaining orange references
4. **Legacy HSL scan:** `grep -rn "hsl(var" src/` — should return zero results
5. **text-black on accent scan:** `grep -rn "text-black" src/` — review each result to confirm none are on accent backgrounds
6. **rounded-2xl scan:** `grep -rn "rounded-2xl" src/` — should return zero results
7. **templateId scan:** `grep -rn "templateId" src/components/rotation/ src/components/workout/` — should return zero results

For any issues found, fix them directly in this task. Common residual issues:
- A stray `bg-orange-*` class somewhere
- A `text-black` that was missed
- A hardcoded color string in a utility file

Document all findings and fixes.
  </action>
  <verify>All 7 scans pass clean. Build succeeds. No orange remnants, no legacy HSL, no text-black on accent, no rounded-2xl, no templateId in rotation/workout components.</verify>
  <done>Automated verification confirms zero color remnants, zero build errors, and zero legacy references.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete blue/teal theme overhaul with bug fixes. The entire app has been migrated from warm orange to cool teal with Apple dark mode aesthetics, including: token system, chart colors, volume zones, text contrast, border radius, and shadow reduction. Plus rotation bug fix.</what-built>
  <how-to-verify>
1. Run `npm run dev` and open the app in browser
2. **Overall aesthetic:** App should feel like Apple dark mode — dark gray backgrounds, teal accents, clean minimal borders
3. **Navigation:** All buttons and interactive elements should show teal accent with readable white text
4. **Quick Start:** If you have a rotation and default gym configured, Quick Start should work without "plan or gym not found" error
5. **Analytics page:** Charts should use teal colors. Volume zones should show a teal gradient (light=under to dark=over), not red/yellow/green
6. **Cards:** Border radius should be subtler (12px not 16px). Shadows should be minimal.
7. **Settings:** All accent buttons should have white text on teal background
8. **Scan for orange:** No orange accent colors should be visible anywhere in the app
  </how-to-verify>
  <resume-signal>Type "approved" if the visual result matches the Apple dark mode teal aesthetic, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. All automated scans pass clean
2. Visual inspection confirms teal aesthetic matches CONTEXT.md description
3. Quick Start flow works without errors
</verification>

<success_criteria>
- Zero build errors
- Zero orange remnants in code or UI
- User approves the visual aesthetic
- Quick Start works
</success_criteria>

<output>
After completion, create `.planning/phases/22-bug-fixes-theme/22-05-SUMMARY.md`
</output>
